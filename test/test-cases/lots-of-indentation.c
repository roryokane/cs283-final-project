int count = 0;
while (count % 3 == 2) {
	int i;
	for (i=0; i<5; i++) {
		count = foo(count);
		if count == 0 {
			printf("count is zero");
		}
	for (i=count; i<count*2; i+=3) {
	}
	printf("calculated");
}
result = i;

if (result) {
	
	while (! result) {
		bar(result);
	}
	if (result == 2) {
		gall(31);
	}
	else if (result > 0) {
		gall(559);
	}
	if (do_check()) {
		printf("it's okay");
	}
		
	
	// the end
}
